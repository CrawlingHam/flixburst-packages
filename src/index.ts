export {
    getSystemThemePreference,
    shouldUseDarkMode,
    applyThemeToDOM,
    initializeTheme,
    setupThemeManagement,
    setTheme,
    subscribeToSystemTheme,
} from './utils';
export type { Theme, ThemeHookOptions, ThemeHookState, SetupThemeManagementParams } from './types';
